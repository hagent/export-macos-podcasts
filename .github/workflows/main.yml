name: Run eslint

on: [push, pull_request]

jobs:  
  test:
    runs-on: ubuntu-latest

    steps:
    - run: echo 1234567
    - run: pwd
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 14
    - run: npm ci
    - run: npm run lint 
